#include <iostream>
// We do not use 'using namespace std;' on purpose

int main()
{
    // Printing on the standard output
    // What is the difference between 'std::endl' and '\n'?
    std::cout << "Hello, FMI!" << std::endl;

    return 0;
}